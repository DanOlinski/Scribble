# This app creates a container with a wordpress app and a mysql database server

# type into the console; `docker-compose up -d` (this will create the images (wp and mysql) and start it up in a container)

